---
import Layout from "@/layouts/BaseLayout";

import { cosmic } from "@/library/cosmic";
import * as fs from "fs";
import * as path from "path";
import { readFile } from "fs/promises";

// UPDATE METADATA
// const folder = "2024-12-12-2018-aston-martin-vanquish";

// const { media: files } = await cosmic.media.find({
// 	folder: folder,
// });

// for (const file of files) {
// 	let originalMetadata = file.metadata;

// 	const replacements = {
// 		cameraMake: "camera_make",
// 		cameraModel: "camera_model",
// 		settingsfStop: "settings_f_stop",
// 		settingsISO: "settings_iso",
// 		settingsShutter: "settings_shutter",
// 		settingsFocalLength: "settings_focal_length",
// 	};

// 	let replacedItems = Object.keys(file.metadata).map((key) => {
// 		const newKey = replacements[key] || key;
// 		return { [newKey]: file.metadata[key] };
// 	});

// 	const newMetadata = replacedItems.reduce((a, b) => Object.assign({}, a, b));

// 	//console.log(replacedItems);
// 	console.log(newMetadata);

// 	await cosmic.media.updateOne(file.id, {
// 		metadata: newMetadata,
// 	});
// }
---

<Layout />
