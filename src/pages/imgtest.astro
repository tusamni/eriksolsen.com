---
// content
import BaseLayout from "@/layouts/BaseLayout";
import { getCollection } from "astro:content";
import Figure from "@/components/image/Figure";

const images = await getCollection("images");
---

<BaseLayout>
    {images.map((i) => <Figure src={i.slug} />)}
</BaseLayout>
