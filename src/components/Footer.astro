---
// components
import ContactForm from "@/components/ContactForm";
import Container from "@/components/content/Container";
import Figure from "@/components/image/Figure";

// config
import { siteConfig } from "@/src/config";

// data
import Navigation from "@/data/Navigation";
---

<Container>
    <div class="mt-24 border-t pt-24">
        <div class="grid gap-8 sm:grid-cols-2 md:gap-12 lg:gap-16">
            <div class="space-y-8 md:py-8">
                <h1>Ready to work together? Let's chat.</h1>

                <div>
                    <ContactForm />
                </div>
            </div>

            <Figure
                src="/shoots/2020/2020-02-28-lit-show-day-two/lit-show-2020_019.jpg"
                metadata={false}
                class="object-cover w-full h-full hidden sm:block"
            />
        </div>

        <footer class="mt-24 border-t pt-10 pb-16 dark:border-zinc-700/40">
            <div class="flex flex-col items-center justify-between gap-6 sm:flex-row">
                <div class="flex gap-6">
                    {
                        Navigation.map((l) => (
                            <a href={l.url} title={l.display}>
                                {l.display}
                            </a>
                        ))
                    }
                </div>

                <div class="flex items-center justify-between gap-4 md:gap-6">
                    <p>
                        <a href="https://www.bible.com/bible/111/JER.29.11.NIV" title="For I know the plans I have for you...">Jeremiah 29:11</a>
                    </p>

                    <p>
                        &copy; {new Date().getFullYear()}
                        {siteConfig.name}
                    </p>
                </div>
            </div>
        </footer>
    </div>
</Container>
