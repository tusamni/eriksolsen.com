---
import Layout from "@/layouts/BaseLayout";

import { cosmic } from "@/library/cosmic";
import { getCollection } from "astro:content";

const allShoots = await getCollection("shoots");
allShoots.map((s) => {
	const obj = {};
});

console.log(allShoots);
---

<Layout />
