---
// components
import { Picture } from "@astrojs/image/components";
import { getEntry } from "astro:content";

// config
import { imageConfig } from "@/src/config";

// props
export interface Props {
    src: string;
    metadata?: boolean;
    sizes?: string;
    figureClass?: string;
}
const { src, metadata = true, sizes = "100vw", figureClass, ...attrs } = Astro.props;

const imageData = await getEntry("images", `${src}.mdx`);

const formats = import.meta.env.MODE === "development" ? imageConfig.imageFormats : imageConfig.imageFormats;
const widths = import.meta.env.MODE === "development" ? imageConfig.imageSizes : imageConfig.imageSizes;
---

<figure class:list={[figureClass]}>
    <Picture
        src={`${imageConfig.cloudUrl}/${imageData.slug}.jpg`}
        alt={imageData.data.title}
        formats={formats}
        aspectRatio={`${imageData.data.width}:${imageData.data.height}`}
        widths={widths}
        sizes={sizes}
        {...attrs}
    />

    {
        metadata ? (
            <figcaption class="mt-4 flex items-center justify-between gap-8 text-xs text-zinc-500 dark:text-zinc-300">
                <span class="line-clamp-1 uppercase">{imageData.data.description}</span>

                <div>
                    <a href={`/photography/photo/${imageData.slug}`} title={imageData.data.title}>
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke-width="1.5"
                            stroke="currentColor"
                            class="h-5 w-5 text-zinc-500 dark:text-zinc-400">
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                d="M11.25 11.25l.041-.02a.75.75 0 011.063.852l-.708 2.836a.75.75 0 001.063.853l.041-.021M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-9-3.75h.008v.008H12V8.25z"
                            />
                        </svg>
                    </a>
                </div>
            </figcaption>
        ) : (
            ``
        )
    }
</figure>
