---
// config
import { siteConfig, imageConfig } from "@/src/config"

// props
export interface Props {
    color?: string,
    orientation?: string,
}
const { color = "dark", orientation = "horizontal" } = Astro.props;
const logoSrc = `${imageConfig.cloudUrl}/logo/${orientation}-${color}.svg`;
---

<style lang="scss">
    a {
        img {
            width: var(--width);
            height: var(--height);
        }
    }
</style>

<a href="/" title={siteConfig.name}>
    <img src={logoSrc} />
</a>