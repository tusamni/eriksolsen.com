---
import Layout from "@/layouts/BaseLayout";

import { cosmic } from "@/library/cosmic";

const { media: cosmicMedia } = await cosmic.media.find({ type: "image/jpeg" }).props("id");
console.log(cosmicMedia.length);
---

<Layout />
