---
// components
import Container from "../content/Container.astro";

// config
import { siteConfig } from "@/src/config";
---

<div class="py-3 bg-sky-50 text-sky-900 text-sm" id="banner">
    <Container>
        <ul class="flex justify-between sm:justify-end gap-8">
            <li>
                <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a>
            </li>
            <li>
                <a href={`tel:${siteConfig.phone.base}`}>
                    {siteConfig.phone.formatted}
                </a>
            </li>
        </ul>
    </Container>
</div>
