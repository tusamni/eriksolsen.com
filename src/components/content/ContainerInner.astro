---
// props
export interface Props {
    classes?: string;
}
const { classes, ...attrs } = Astro.props;
---

<div class:list={["relative px-4 sm:px-8 lg:px-12", classes]} {...attrs}>
    <div class="mx-auto max-w-2xl lg:max-w-5xl"><slot /></div>
</div>
