---
// props
export interface Props {
	size?: "small" | "large";
	classes?: string;
}
const { size = "small", classes } = Astro.props;
---

<div class:list={[size ? "space-y-12 sm:space-y-24" : "space-y-16 sm:space-y-40", classes]}>
	<slot />
</div>
