---
// components
import Container from "@/components/Container";

// data
import Navigation from "@/data/Navigation";

// functions
import { getImage } from "@/functions/Media";

const logo = await getImage("/logos/logo-black.png");
---

<div class="sticky top-0 z-10 bg-clip-padding backdrop-filter backdrop-blur-xl bg-white/90">
    <div class="py-6 md:py-8">
        <Container>
            <nav class="flex flex-col sm:flex-row justify-between sm:items-center tracking-normal gap-4">
                <a href="/" title="Erik S. Olsen" class="place-self-center">
                    <img src={logo.src} width="1000" height="234" class="w-40 md:w-52" />
                </a>

                <div class="flex justify-center divide-x">
                    {
                        Navigation.map((l) => (
                            <a
                                href={l.url}
                                title={l.display}
                                class="px-3 text-stone-800 underline decoration-transparent underline-offset-4 transition duration-200 first:pl-0 last:pr-0 hover:decoration-stone-800 sm:px-5 md:px-8"
                            >
                                {l.display}
                            </a>
                        ))
                    }
                </div>
            </nav>
        </Container>
    </div>
</div>
