---
import Layout from "@/layouts/BaseLayout";

import { cosmic } from "@/library/cosmic";

const allMedia = [];

let count = 0;
//for (let i = 0; i < 9; i++) {}
const { media: cosmicMedia } = await cosmic.media.find({ type: "image/jpeg" }).props("id").skip(2);
---

<Layout />
